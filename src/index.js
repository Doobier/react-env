import React from 'react';
import ReactDom from 'react-dom';
// import PouterConfig from 'router/router';

function PouterConfig(){
    return (
        <div>hello react111</div>
    )
}
ReactDom.render(<PouterConfig />, document.getElementById('app'));